---
en:
  errors:
    format: "%{message}"
    messages:
      accepted: "%{attribute} must be accepted"
      blank: "%{attribute} can't be blank"
      confirmation: "%{attribute} doesn't match"
      empty: "%{attribute} can't be empty"
      equal_to: "%{attribute} must be equal to"
      even: "%{attribute} must be even"
      exclusion: "%{attribute} is reserved"
      greater_than: "%{attribute} must be greater than %{count}"
      greater_than_or_equal_to: "%{attribute} must be greater than or equal to %{count}"
      inclusion: "%{attribute} is not included in the list"
      invalid: "%{attribute} has invalid format"
      invalid_url: "%{attribute} is not a valid url"
      less_than: "%{attribute} must be less than %{count}"
      less_than_or_equal_to: "%{attribute} must be less than or equal to %{count}"
      not_a_number: "%{attribute} is not a number"
      not_an_integer: "%{attribute} must be an integer"
      odd: "%{attribute} must be odd"
      other_than: "%{attribute} must be other than %{count}"
      present: "%{attribute} must be blank"
      record_invalid: "Validation failed: %{errors}"
      restrict_dependent_destroy:
        has_one: "Cannot delete record because a dependent %{record} exists"
        has_many: "Cannot delete record because a dependent %{record} exists"
        ? ""
        :
          one: "Cannot delete record because a dependent %{record} exists"
          other: ""
      required: "%{attribute} can't be empty"
      taken: "%{attribute} has already been taken"
      too_long: "%{attribute} is too long (maximum is %{count} characters)"
      too_short: "%{attribute} is too short (minimum is %{count} characters)"
      wrong_length: "%{attribute} has the wrong length (should be %{count} characters)"
    attributes:
      email:
        blank: "E-mail address is required"
      phone:
        blank: "Phone number is required"
      name:
        blank: "First name is required"
      lastname:
        blank: "Last name is required"
      post_id:
        blank: "A position is required"
    template:
      body: "There were problems with the following fields:"
      header:
        one: "1 error prohibited this %{model} from being saved"
        other: "%{count} errors prohibited this %{model} from being saved"
  activemodel:
    errors:
      format: "%{message}"
      messages:
        accepted: "%{attribute} must be accepted"
        blank: "%{attribute} must not be blank"
        confirmation: "%{attribute} does not match"
        empty: "%{attribute} can't be empty"
        equal_to: "%{attribute} must be equal to"
        even: "%{attribute} must be even"
        exclusion: "%{attribute} is reserved"
        greater_than: "%{attribute} must be greater than %{count}"
        greater_than_or_equal_to: "%{attribute} must be greater than or equal to %{count}"
        inclusion: "%{attribute} is not included in the list"
        invalid: "%{attribute} has invalid format"
        invalid_url: "%{attribute} is not a valid url"
        less_than: "%{attribute} must be less than %{count}"
        less_than_or_equal_to: "%{attribute} must be less than or equal to %{count}"
        not_a_number: "%{attribute} is not a number"
        not_an_integer: "%{attribute} must be an integer"
        odd: "%{attribute} must be odd"
        other_than: "%{attribute} must be other than %{count}"
        present: "%{attribute} must be blank"
        record_invalid: "Validation failed: %{errors}"
        restrict_dependent_destroy:
          has_one: "Cannot delete record because a dependent %{record} exists"
          has_many: "Cannot delete record because a dependent %{record} exists"
          ? ""
          :
            one: "Cannot destroy object as there is a dependency %{record}"
            other: "Cannot destroy object as there is a dependency %{record}"
        required: "%{attribute} can't be blank"
        taken: "%{attribute} already in use"
        too_long: "%{attribute} is too long (maximum is %{count} characters)"
        too_short: "%{attribute} is too short (minimum is %{count} characters)"
        wrong_length: "%{attribute} is the wrong length (should be %{count} characters)"
        end_after_start: "End date must be after start date"
      attributes:
        email:
          blank: "E-mail address is required"
        phone:
          blank: "Phone number is required"
        name:
          blank: "First name is required"
        lastname:
          blank: "Last name is required"
        post_id:
          blank: "A position is required"
      template:
        body: "There were problems with the following fields:"
        header:
          one: "An error prohibited this %{model} from being saved"
          other: "%{count} errors prohibited this %{model} from being saved"
  activerecord:
    errors:
      format: "%{message}"
      messages:
        accepted: "%{attribute} must be accepted"
        blank: "%{attribute} can't be blank"
        confirmation: "%{attribute} doesn't match"
        empty: "%{attribute} can't be empty"
        equal_to: "%{attribute} must be equal to"
        even: "%{attribute} must be even"
        exclusion: "%{attribute} is reserved"
        greater_than: "%{attribute} must be bigger than %{count}"
        greater_than_or_equal_to: "%{attribute} has to be bigger than or equal to %{count}"
        inclusion: "%{attribute} is not in the list"
        invalid: "%{attribute} is not valid"
        invalid_url: "%{attribute} is not a valid url"
        less_than: "%{attribute} must be less than %{count}"
        less_than_or_equal_to: "%{attribute} must be less than or equal to %{count}"
        not_a_number: "%{attribute} is not a number"
        not_an_integer: "%{attribute} must be an integer"
        odd: "%{attribute} must be odd"
        other_than: "%{attribute} must not be %{count}"
        present: "%{attribute} must be blank"
        record_invalid: "Validation failed: %{errors}"
        restrict_dependent_destroy:
          has_one: "Cannot delete record because a dependent %{record} exists"
          has_many: "Cannot delete record because a dependent %{record} exists"
          ? ""
          :
            one: "Cannot delete record because a dependent %{record} exists"
            other: "Cannot delete record because a dependent %{record} exists"
        required: "%{attribute} can't be blank"
        taken: "%{attribute} already in use"
        too_long: "%{attribute} is too long (maximum is %{count} characters)"
        too_short: "%{attribute} is too short (minimum is %{count} characters)"
        wrong_length: "%{attribute} has the wrong length (should be %{count} characters)"
        end_after_start: "End date must be after start date"
      attributes:
        email:
          blank: "Email is required"
        phone:
          blank: "Phone number is required"
        name:
          blank: "First name is required"
        lastname:
          blank: "Surname is required"
        post_id:
          blank: "A position must be entered"
      template:
        body: "There is a problem with this field:"
        header:
          one: "An error prevented this %{model} from being saved"
          other: "%{count} errors prevented this %{model} from being saved"
