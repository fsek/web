<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
  	<h3> Hej <%=@candidate.profile.name %></h3>
			<p>Du har kandiderat till sektionsposten <%=@candidate.post.title%>.</p>
			<%if (@candidate.election.candidate_mail) && (@candidate.post.elected_by == "Valberedning")%>
				<%= simple_format(@valet.candidate_mail)%>
			<%end%>
			<%if (@candidate.post.styrelse) && (@candidate.election.extra_text)%>
				<%= simple_format(@valet.extra_text)%>	
			<%end%>				
			<%if @valet.mail_styrelse_link%> <%=url_for(@valet.mail_styrelse_link)%> <%else%> <%=url_for(@valet.mail_link)%> <%end%><br>
	    	<p>Valet stänger <%= @valet.end%>,
	    	fram tills dess kan du redigera din kandidatur på:<br><%= url_for(host: PUBLIC_URL, controller: 'elections',action: 'candidate') %></p>
	    	
	    	<p>Mvh,<br>
	    	Valberedningen</p>   
  </body>
</html>