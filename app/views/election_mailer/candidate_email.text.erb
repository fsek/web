Hej <%=@candidate.profile.name %>
===============================================
Du har kandiderat till sektionsposten <%=@candidate.post.title%>.
<%if @candidate.post.elected_by == "Terminsmötet"%>
				<%if (@candidate.election.candidate_mail)
					<%= simple_format(@candidate.election.candidate_mail)%>
				<%end%>
				<%if (@candidate.post.styrelse)%>
					<%=url_for(@candidate.election.mail_styrelse_link)%><br><br> 
				<%else%>
				 	<%=url_for(@candidate.election.mail_link)%><br><br>
				<%end%>
			<%else%>
				<%= simple_format(@candidate.election.candidate_mail_star)%>
			<%end%>
Valet stänger <%= @candidate.election.end%>,
fram tills dess kan du redigera din kandidatur på:
<%= url_for(host: PUBLIC_URL, controller: 'elections',action: 'candidate') %>

Mvh,
Valberedningen