<div class="col-md-8 col-md-offset-2 col-sm-12 reg-page">
  <div class="headline">
    <h2>
      <%= t(:new_attr, attr: model_name(Page)) %>
    </h2>
  </div>
  <%= render 'form' %>
  <hr>
  <%= link_to(t(:index, attr: models_name(Page)), pages_path,
              class: 'btn secondary') %>
</div>
