<%if @main%>
	<%if @edit%>
		<div class="reg-page">
	<%end%>
	<% if @main.headline.blank? == false%>
		<div class="headline"><h2><%= @main.headline %></h2></div>
	<% end %>
	<% if @main.text.blank? == false%>
		<%=simple_format(@main.text)%>
	<%end%>
	
	<%if @edit%>
		<hr>
		<ul class="list-inline list-unstyled black-link" style="margin-top: 10px; text-align: left">
			<li>Namn: <%=@main.name%></li>
			<li>Visningsindex: <%=@main.displayIndex%></li>
			<li><%if @main.visible%>Synlig<%else%>Ej synlig<%end%>
			<li><%= link_to (content_tag(:i, nil, class: "fa fa-edit") + " Redigera"), edit_council_page_page_element_path(@council,@main), class: "btn-u btn-u-small"%></li>		
		</ul>
	<%end%>
 	<%if @edit%>
 		</div>
 	<%end%>
<%end%>
