<%= simple_form_for(@page) do |f| %>
  <%= f.input :title %>
  <%= f.input :url %>
  <%= f.input :visible, as: :boolean %>
  <%= f.input :namespace %>
  <%= f.input :council, collection: Council.all %>
  <%= f.submit t(:save, attr: Page.model_name.human), class: 'btn btn-u' %>
<% end %>
