<%= simple_form_for(@squad) do |f| %>
  <%= f.input :title %>
  <%= f.input :category, collection: ['Nollning', 'Övrigt'] %>
  <%= f.input :description, as: :text %>
  <%= f.button :submit %>
<% end %>
