<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h2><%= t('contact.message_sent_via') %> - <%= @contact.to_s %></h2>
    <table>
      <tr>
        <td><%= ContactMessage.human_attribute_name(:name) %></td>
        <td><%= @contact.message.name %></td>
      </tr>
      <tr>
        <td><%= ContactMessage.human_attribute_name(:email) %></td>
        <td><%= @contact.message.email %></td>
      </tr>
      <tr>
        <td><%= ContactMessage.human_attribute_name(:message) %></td>
        <td><%= simple_format(@contact.message.message) %></td>
      </tr>
    </table>
  </body>
</html>
