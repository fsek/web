<% if events.count > 0 %>
  <div class="event stream">
    <% events.each do |e| %>
      <%= content_tag(:span, e, class: 'title') %>
      <%= content_tag(:span, e.print_date, class: 'date') %>
      <%= content_tag(:span, :wq

    <% end %>
  </div>
<% end %>
