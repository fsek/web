<% if @songs.present? %>
  $('#song-status').html('');
  $('#song-table').html('<%= j render('search_row', songs: @songs) %>');
<% else %>
  $('#song-status').html("<%= t('.no_results') %>");
  $('#song-table').html('');
<% end %>
