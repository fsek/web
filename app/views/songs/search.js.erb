<% if @songs.present? %>
  $('#song-status').html('');
  $('#song-table').html('<%= j render('search_row', songs: @songs) %>');
<% else %>
  $('#song-status').html("<%= I18n.t('songs.search_bar.no_results') %>");
  $('#song-table').html('');
<% end %>
