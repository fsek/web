<%= simple_form_for([:admin, @cafe_work]) do |f| %>
  <%= f.input :work_day, as: :datetime_picker %>
  <%= f.input :pass %>
  <%= f.input :lp %>
  <%= f.input :lv %>
  <%= f.button :submit, class: 'btn btn-u' %>
<% end %>
