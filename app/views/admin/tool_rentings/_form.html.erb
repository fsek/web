<% if rent.present? %>
  <%= simple_form_for([:admin, rent.tool, rent]) do |f|%>
    <%= f.input :returned, as: :boolean %>
    <%= f.input :renter %>
    <%= f.input :purpose %>
    <%= f.input :return_date, as: :datetime_picker%>
    <%= f.button :submit %>
    <% if rent.persisted? %>
      <%= link_to(t('.destroy'), admin_tool_tool_renting_path(rent.tool, rent),
                                 method: :delete, 
                                 class: 'btn danger pull-right') %>
    <% end %>
  <% end %>
<% end %>
