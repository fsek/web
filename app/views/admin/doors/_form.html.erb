<%= simple_form_for([:admin, door]) do |f| %>
  <%= f.input :title %>
  <%= f.input :slug %>
  <%= f.input :description %>
  <%= f.association :posts, collection: Post.by_title, input_html: {class: 'select2_tags'} %>
  <%= f.button :submit %>
<% end %>
