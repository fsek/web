<div class="col-md-10 col-md-offset-1 col-sm-12 reg-page">
  <div class="headline">
    <h3><%= title(t('.title')) %></h3>
  </div>

  <%= render('form', song_category: @song_category) %>
  <hr>
  <%= link_to(t('.all_song_categories'), admin_song_categories_path, class: 'btn secondary') %>
</div>
