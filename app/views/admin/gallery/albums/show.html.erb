<div class="headline headline-md">
  <h1><%= @album.title %></h1>
  <%= link_to(t(:edit), edit_admin_gallery_album_path(@album), class: 'btn primary') %>
</div>

<ul class="row">
  <% @album.images.each do |p| %>
    <%= image_tag(p.view, class: 'img-responsive') %>
  <% end %>
</ul>
