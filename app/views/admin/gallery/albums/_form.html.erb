<%= simple_form_for([:admin,:gallery, @album]) do |f| %>
  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :location %>
    <%= f.input :start_date, as: :datetime_picker %>
    <%= f.input :end_date, as: :datetime_picker %>
    <%= f.input :public %>
  </div>
  <% unless @album.new_record? %>
    <div class="form-files">
      <%= f.input :images_upload, as: :file, input_html: { multiple: true } %>
      <%= f.input :images_photographer, as: :select, collection: @users %>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.button :submit %>
  </div>
<% end %>
