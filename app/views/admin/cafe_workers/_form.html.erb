<%= simple_form_for([cafe_shift, cafe_shift.cafe_worker], url: admin_cafe_shift_path(cafe_shift)) do |f| %>
  <%= f.association :user, collection: [current_user] %>
  <%= f.input :competition, as: :boolean %>
  <%= f.association :councils, as: :check_boxes %>
<% end %>
