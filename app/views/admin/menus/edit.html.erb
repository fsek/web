<div class="col-md-6 col-md-offset-3 col-xs-12 reg-page">
  <div class="headline">
    <h3><%= title(t('menu.edit')) %></h3>
  </div>
  <%= render('form', menu: @menu) %>
  <hr>
  <%= link_to(t('menu.destroy'), admin_menu_path(@menu),
              method: :delete, class: 'btn danger pull-right') %>
  <%= link_to(t('menu.index'), admin_menus_path, class: 'btn secondary') %>
</div>
