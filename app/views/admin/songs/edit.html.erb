<div class="col-md-10 col-md-offset-1 col-sm-12 reg-page">
  <div class="headline">
    <h1><%= title(t('.title')) %></h1>
  </div>
  <%= render('form', song: @song) %>
  <hr>
  <%= link_to(t('.delete'), admin_song_path(@song),
              method: :delete, class: 'btn danger pull-right') %>
  <%= link_to(t('.all_songs'), admin_songs_path, class: 'btn secondary') %>
</div>
