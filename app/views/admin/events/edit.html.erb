<div class="col-md-6 col-md-offset-4 col-xs-12 reg-page">
  <h3 class="headline headline-md">
    <%= t(:edit, attr: model_name(Event)) %>
  </h3>
  <%= render 'form'%>
  <hr>
  <%= link_to(t(:show, attr: model_name(Event)), admin_event_path(@event)) %><br>
  <%= link_to(t(:index, attr: models_name(Event)), :admin_events) %>
</div>
