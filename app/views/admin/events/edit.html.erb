<div class="col-md-8 col-md-offset-2 col-xs-12 reg-page">
  <div class="headline">
    <h3><%= t('event.edit') %></h3>
  </div>
  <%= render 'form'%>
  <hr>
  <%= link_to(t('event.destroy'),
              admin_event_path(@event), method: :delete,
              class: 'btn danger pull-right') %>
  <%= link_to(t('event.show'),
              event_path(@event), class: 'btn secondary') %>
  <%= link_to(t('event.index'),
              admin_events_path, class: 'btn secondary') %>
</div>
