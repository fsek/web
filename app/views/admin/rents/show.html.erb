<% provide(:title, 'Admin - Bilbokning') %>
<div class="col-md-6 col-md-offset-3 reg-page">
  <%= render 'links' %>
  <%= render 'rent', rent: @rent %>
  <%= render 'status_form' %>
  <div class="row">
    <%= link_to('Redigera bokning', edit_admin_rent_path(@rent), class: 'btn btn-u') %> <%= link_to('Radera bokning', admin_rent_path(@rent), method: :delete, class: "btn btn-u", data: {confirm: 'Din bokning kommer nu att raderas.'}) %>
  </div>
</div>