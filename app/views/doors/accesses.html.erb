<pre style= "word-wrap: break-word; white-space: pre-wrap;">
<% @door.users.pluck(:student_id).each do |a| %>
<%= if a != nil && a != ""
    a + "\n"
    end -%>
<% end %>
<% @temp.each do |b| %>
<%= if b != nil && b != ""
User.find(b). student_id + "\n"
end -%>
<% end %>
</pre>
