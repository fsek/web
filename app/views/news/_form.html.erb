<%= simple_form_for(@news) do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <% if @news.image.present? %>
    <%= image_tag(@news.image(:thumb)) %>
  <% end %>
  <%= f.input :image %>
  <%= f.button :submit %>
<% end %>
