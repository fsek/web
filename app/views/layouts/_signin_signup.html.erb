<li>
  <%= link_to(new_user_session_path) do %>
    <span class="topbar_icon"><%= fa_icon('sign-in') %></span>
    <span class="topbar_text"><%= t('devise.sign_in') %></span>
  <% end %>
  </li>
  <li class="divider"></li>
  <li>
  <%= link_to(new_user_registration_path) do %>
    <span class="topbar_icon"><%= fa_icon('plus') %></span>
    <span class="topbar_text"><%= t('devise.sign_up') %></span>
  <% end %>
</li>
