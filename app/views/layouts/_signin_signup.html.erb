<li>
  <%= link_to(new_user_session_path) do %>
    <span class="topbar-icon"><%= icon('fas', 'user') %></span>
    <span class="topbar-text"><%= t('devise.sign_in') %></span>
  <% end %>
  </li>
  <li class="divider"></li>
  <li>
  <%= link_to(new_user_registration_path) do %>
    <span class="topbar-icon"><%= icon('fas', 'user-plus') %></span>
    <span class="topbar-text"><%= t('devise.sign_up') %></span>
  <% end %>
</li>
