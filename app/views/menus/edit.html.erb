<div class="col-md-6 col-md-offset-3 col-xs-12 reg-page">
  <div class="headline">
    <h3><%= title(t('.title')) %></h3>
  </div>
  <%= render('form') %>
  <hr>
  <%= link_to(t('.destroy'), menu_path(@menu),
                             method: :delete,
                             class: 'btn danger pull-right') %>
  <%= link_to(t('.index'), menus_path,
                           class: 'btn secondary') %>
</div>
