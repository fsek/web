<%= simple_form_for(user, url: password_own_user_path) do |f| %>
  <div class="headline">
    <h3><%= t('.title') %></h3>
  </div>
  <%= f.input :password  %>
  <%= f.input :password_confirmation %>
  <%= f.input :current_password %>
  <%= f.button :submit, id: 'user-password-submit' %>
<% end %>
