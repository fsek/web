<div class="col-md-6 col-md-offset-3 col-xs-12 reg-page">
  <div class="headline">
    <h2><%= t('candidate.new') %></h2>
  </div>
  <%= render 'form', election_view: @election_view %>
  <hr>
  <%= link_to(t(:back), candidates_path, class: 'btn secondary') %>
</div>
