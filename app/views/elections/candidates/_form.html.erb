<% if election_view.present? %>
  <%= simple_form_for(election_view.candidate) do |f| %>
      <%= f.association :user, collection: [ current_user ], include_blank: false %>
    <% if election_view.candidate.new_record? %>
      <%= f.association(:post, collection: election_view.election.posts.title,
                               include_blank: false,
                               input_html: { class: 'select2' }) %>
      <%= f.button :submit, id: 'candidate-submit' %>
    <% else %>
      <h4><%= election_view.candidate.post %></h4>
      <%= simple_format(t('candidate.cannot_change_post')) %>
      <%= link_to(t('candidate.destroy'), candidate_path(election_view.candidate),
                                          method: :delete,
                                          class: 'btn danger') %>
    <% end %>
  <% end %>

  <hr>

  <%= simple_format(t('candidate.user_attributes')) %>
  <%= link_to(t('user.edit'), edit_own_user_path,
                              target: :blank,
                              class: 'btn secondary') %>

  <table class="table">
    <tbody>
      <%= render '/users/as_table', user: current_user, prot: true %>
    </tbody>
  </table>
<% end %>
