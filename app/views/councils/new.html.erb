<div class="col-md-8 col-md-offset-2 col-sm-12 reg-page">
  <div class="headline">
    <h3><%= t('council.new') %></h3>
  </div>

  <%= render 'form', council: @council %>
  <hr>
  <%= link_to(t('council.index'), councils_path,
                                  class: 'btn secondary')%>
</div>
