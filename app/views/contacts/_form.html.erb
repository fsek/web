<%= simple_form_for(@contact) do |f| %>
  <%= f.input :name %>
  <%= f.input :text %>
  <%= f.input :email %>
  <%= f.input :public %>
  <%= f.button :submit, class: "btn btn-u", id: "contact-submit" %>
<% end %>
